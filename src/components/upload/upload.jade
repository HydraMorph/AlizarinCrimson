// Draft - it doesn't work
md-bottom-sheet.md-grid.uploadbar
  md-button Upload - Drag & Drop here!

  md-list
    md-item(ng-repeat="item in items")
      md-button.md-grid-item-content(ng-click="listItemClick($index)" aria-label="{{ item.name }}")
        div.md-icon-container
          md-inline-grid-icon(icon="{{ item.icon }}")
